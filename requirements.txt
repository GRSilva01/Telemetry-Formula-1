# ================= REQUERIMENTOS DO PROJETO =================
# Dependências com versões fixadas para ambiente de produção/portfólio

# Interface gráfica e plotagem
tkinter            # Já vem incluso no Python (não precisa instalar)
matplotlib         >=3.6.0
numpy              >=1.24.0
scipy              >=1.10.0

# Dados e análise
pandas             >=2.1.0

# FastF1 - Dados oficiais da FIA
fastf1             >=1.10.0

# Banco de dados relacional (SQLite via SQLAlchemy)
sqlalchemy         >=2.0.0

# Gerenciamento de ambiente (opcional)
python-dotenv      >=1.0.0